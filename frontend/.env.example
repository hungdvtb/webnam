# Leave the admin UI on same-origin `/api` by default.
# In local Vite dev, `/api`, `/media`, and `/storage` are proxied to VITE_DEV_API_PROXY_TARGET.
VITE_API_BASE_URL=/api
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:8003
VITE_STORAGE_BASE_URL=/
VITE_PUBLIC_SITE_URL=http://localhost:3000
